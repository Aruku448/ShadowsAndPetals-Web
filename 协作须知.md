# 协作须知

本文面向参与本网站内容维护的协作者，说明默认内容 JSON 的运行方式，以及如何安全地修改、验证和提交内容。

## 一句话说明

页面默认内容由仓库内的 `config/ashfall-home-config.json` 提供。手动编辑该 JSON 并刷新页面，即可看到变化。

## JSON 运行方式

### 加载顺序

页面启动时按以下优先级决定内容来源：

1. 浏览器 `localStorage` 中的 `ashfall-home-config-v2`（如果存在）
2. 仓库内的 `config/ashfall-home-config.json`
3. `app.js` 内置的 `defaults` 兜底

也就是说：**只要浏览器里已经存过配置，JSON 就不会被读取。**

### 具体行为

- 页面通过 `http(s)` 打开时，如果 `localStorage` 为空，会请求 `./config/ashfall-home-config.json`。
- 请求使用 `cache: "no-store"`，所以每次刷新都会重新读取 JSON。
- 如果 JSON 不存在或解析失败，自动回退到 `app.js` 内置默认内容。
- 通过 `file://` 直接打开页面时不会读取 JSON，必须使用开发服务器。
- 读取到的 JSON 都会经过 `mergeConfig()` 补齐新字段，旧配置不会因为缺少字段而报错。

## 本地开发环境

启动开发服务器：

```bash
npm run dev
```

默认端口为 `8088`。如果端口被占用，可换端口启动：

```bash
PORT=8091 npm run dev
```

然后浏览器打开 `http://localhost:8091`（或对应端口）。

## 推荐工作流程

### 方式一：直接手动编辑 JSON

1. 编辑 `config/ashfall-home-config.json`。
2. 启动开发服务器并打开页面。
3. 确认浏览器没有该站点的 `localStorage` 配置；如有，请清除站点数据或使用无痕窗口，否则 `localStorage` 会覆盖 JSON。
4. 刷新页面验证内容。
5. 提交 `config/ashfall-home-config.json`；如果引用了 `assets/uploads/` 中的新图片，一并提交。

### 方式二：先用编辑器调整，再固化为默认 JSON

1. 打开页面，点击“自定义”进入编辑器。
2. 修改内容会自动保存到浏览器 `localStorage`。
3. 需要把结果固化为默认内容时，点击“导出 JSON”。
4. 将下载的文件覆盖为仓库内的 `config/ashfall-home-config.json`。
5. 提交该文件。

注意：一旦浏览器存在 `localStorage`，后续对 JSON 的修改可能看不到效果，因为 `localStorage` 优先级更高。

## 图片路径

- JSON 中的图片字段保存相对路径（如 `assets/uploads/example.jpg`）或公开 HTTPS 图床地址。
- 编辑器上传的图片会写入 `assets/uploads/`，这些文件需要一并提交到仓库。

## 验证清单

修改 JSON 后至少确认：

- `npm run check` 通过。
- 浏览器刷新后内容与 JSON 一致。
- `git diff --check` 无空白错误。
- 桌面端和移动端没有明显溢出或错位。

## 相关文档

- `DEVELOPER_GUIDE.md`：完整的项目维护指南。
- `AGENTS.md`：项目维护入口。
