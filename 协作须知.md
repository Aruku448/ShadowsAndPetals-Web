# 协作须知

本文面向参与本网站内容维护的协作者，说明默认内容 JSON 的运行方式，以及如何安全地修改、验证和提交内容。

## 一句话说明

页面默认内容由仓库内的 `config/ashfall-home-config.json` 提供。手动编辑该 JSON 并刷新页面，即可看到变化；也可以在编辑器“配置”页签点击“保存到 config JSON”直接写回仓库文件（写入时会自动标注 `configMeta` 版本号）。

## configMeta 版本标注

每次通过编辑器“保存到 config JSON”或“导出 JSON”时，都会在 JSON 顶部写入：

```json
"configMeta": { "revision": 12, "updatedAt": "2026-09-03 12:00" }
```

- `revision` 每次保存递增，方便在 GitHub 上确认协作者的更改已同步。
- `updatedAt` 是保存时间（本地时间）。
- 该字段由 `mergeConfig()` 透传，页面渲染不使用它；手动编辑 JSON 时可以不写，下次从编辑器保存时会自动补上。

## JSON 运行方式

### 加载顺序

页面启动时按以下优先级决定内容来源：

1. 浏览器 `localStorage` 中的 `ashfall-home-config-v2`（**仅本地开发环境**：`localhost` / `127.0.0.1`）
2. 仓库内的 `config/ashfall-home-config.json`
3. `app.js` 内置的 `defaults` 兜底

也就是说：**部署环境（GitHub Pages 等非 localhost）始终读取仓库 JSON**，不读 localStorage——协作者更新 JSON 并推送后，部署站刷新即可看到新内容。本地开发环境只要浏览器里已经存过配置，JSON 就不会被读取。

### 具体行为

- 页面通过 `http(s)` 打开时，如果 `localStorage` 为空，会请求 `./config/ashfall-home-config.json`。
- 请求使用 `cache: "no-store"`，所以每次刷新都会重新读取 JSON。
- 如果 JSON 不存在或解析失败，自动回退到 `app.js` 内置默认内容。
- 通过 `file://` 直接打开页面时不会读取 JSON，必须使用开发服务器。
- 读取到的 JSON 都会经过 `mergeConfig()` 补齐新字段，旧配置不会因为缺少字段而报错。

## 本地开发环境

启动开发服务器：

```bash
npm run dev
```

默认端口为 `8091`。如果端口被占用，可换端口启动：

```bash
PORT=8092 npm run dev
```

然后浏览器打开 `http://localhost:8091`（或对应端口）。

## 推荐工作流程

### 方式一：直接手动编辑 JSON

1. 编辑 `config/ashfall-home-config.json`。
2. 启动开发服务器并打开页面。
3. 确认浏览器没有该站点的 `localStorage` 配置；如有，请清除站点数据或使用无痕窗口，否则 `localStorage` 会覆盖 JSON。
4. 刷新页面验证内容。
5. 提交 `config/ashfall-home-config.json`；如果引用了 `assets/uploads/` 中的新图片，一并提交。

### 方式二：先用编辑器调整，再固化为默认 JSON

1. 打开页面，点击“自定义”进入编辑器。
2. 修改内容会自动保存到浏览器 `localStorage`。
3. 确认内容满意后，在“配置”页签点击“保存到 config JSON”，直接写入仓库内的 `config/ashfall-home-config.json`（自动递增 `configMeta.revision`）；如果本地服务不可用，可退回“导出 JSON”再手动覆盖。
4. 提交该文件（`git diff` 中确认 `configMeta` 变化即可看到本次改动）。

注意：一旦浏览器存在 `localStorage`，后续对 JSON 的修改可能看不到效果，因为 `localStorage` 优先级更高。写入文件的是编辑器当前完整状态（含 `localStorage` 中积累的全部修改），不只是本次改动。

## 图片路径

- JSON 中的图片字段保存相对路径（如 `assets/uploads/example.jpg`）或公开 HTTPS 图床地址。
- 编辑器上传的图片会写入 `assets/uploads/`，这些文件需要一并提交到仓库。

## 错误案例：修改文章导航页时误伤全站图片与既有改动

### 现象

在为文章导航页（`?view=pages&page=1`）增加分页无刷新切换和头图效果时，修改范围没有严格限定在文章导航页，导致全站图片表现异常，并增加了不必要的渲染和样式覆盖风险。

### 原因

- 在 `app.js` 的统一 `render()` 流程中加入文章导航页逻辑；分页切换时会连带执行首页新闻、系统集合、自定义元素和元素样式注册。
- 在 `styles.css` 文件后部追加全局或高优先级图片规则，覆盖了原有 `.subpage-hero-media`、`.directory-hero-media` 以及图片尺寸、`object-fit`、动画等行为。
- 修改动态渲染的 HTML 时，同时改变了子页头图的 `is-loaded`、入口链接和容器结构，超出了“仅修改导航页头图”的范围。
- 未在每轮修改后立即检查 `git diff` 的实际影响范围，也未先建立可回滚的明确基线。

### 防止再次发生

1. 修改前先记录基线：执行 `git status --short` 和 `git diff -- app.js styles.css index.html config/ashfall-home-config.json`，区分用户已有修改与本次修改。
2. 文章导航页专用代码必须使用明确的 `.page-directory` 或 `[data-page-directory]` 作用域；不要用裸的 `img`、`.subpage-hero-media` 或通用 `.directory-*` 规则去覆盖其他页面。
3. 不要为了局部效果改动共享的 `render()`、`renderNews()`、`renderExpertise()`、`renderCustomElements()` 或共享图片类；必要时使用独立函数和独立 class。
4. 动态插入头图时，保持原有图片字段、`src`、`data-*` 属性、`is-loaded` 和加载流程不变；只新增导航页所需的节点。
5. CSS 新增规则放在专用作用域下，并检查文件后半段覆盖规则；修改图片布局前确认 `width`、`height`、`aspect-ratio`、`object-fit` 和 `position` 不会匹配首页或子页。
6. 每次修改后先运行 `git diff --check`、`npm run check`，再用 `git diff --stat` 和 `git diff` 确认没有出现未计划的全局图片、配置或页面结构改动。
7. 涉及图片、布局或动效时，必须在浏览器至少验证：首页、文章导航页、文章详情页，以及桌面和移动端；发现全站图片异常时，先停止继续叠加修改，回到本轮基线按文件和功能逐项回滚。

### 文章导航专辑墙专项规则

文章导航页的头图专辑墙是一个高风险区域，原因是它同时涉及动态插入、旋转后的溢出范围、重复图片和横向动画。维护时必须遵守以下规则：

- 专辑墙只能由 `renderPageDirectory()` 创建，节点必须插入 `[data-page-directory] .directory-hero`；不得插入首页 `main > section`、`.subpage-view` 或复用子页头图节点。
- 专辑墙的实际可视区域必须被 `.directory-hero-cover` 的 `overflow: hidden` 裁切；禁止让墙体自身撑开 `.page-directory`、`main` 或页面滚动高度。
- 旋转只允许作用于静态墙体容器（例如 `.directory-hero-wall { transform: rotate(-30deg) ... }`），水平滚动只允许作用于行轨道（例如 `.directory-hero-wall-track`）；不要把旋转写进 `@keyframes`，也不要让同一个元素同时承担布局旋转和位移动画。
- 为覆盖旋转后的四角，可以让墙体适度向外扩展，但必须以头图为基准控制 `inset` 和 `scale`。扩展过大、增加过多行或把多行内容放在正常文档流中，都会造成“图片堆叠占满页面”的问题。
- 图片应在固定行数的轨道内紧密排列（`gap: 0`），通过横向重复素材实现连续滚动；不要为每个分页重新追加节点，也不要把文章卡片图片容器当作专辑墙容器。
- 专辑墙图片必须是装饰内容，使用空 `alt` 并设置 `aria-hidden="true"`；标题、返回链接和其他交互内容必须保持在更高层级。
- 专辑墙 CSS 应集中在同一处，并至少检查 `.directory-hero-wall`、`.directory-hero-wall-track`、`.directory-hero-wall-tile`、`.directory-hero-overlay` 与 `.directory-hero-media` 是否互相覆盖。不要修改共享的 `.subpage-hero-media` 或通用 `img` 规则。
- 修改绘制区域时，优先调整墙体的 `inset`、`scale`、行数和单张封面宽度；不要通过增加头图高度或修改 `.page-directory` 高度来“填满”区域。
- 专辑墙的滚动动画不得影响分页切换的无动画要求。分页切换后应只重建导航页头图内部内容，并在头图底部被固定导航栏覆盖的位置停留。

验证专辑墙至少检查：

1. `?view=pages&page=1` 和最后一页的头图都只占一个头图区域。
2. 横向滚动时上下没有空白，旋转后的四角没有露出页面底色。
3. 页面总高度没有因为专辑墙增加异常长的空白区。
4. 点击分页后没有再次播放页面进入动画，滚动位置仍位于头图底部。
5. 首页和 `?page=<slug>` 文章详情页的图片尺寸、裁切和动画保持不变。
6. 桌面端、窄屏和 `prefers-reduced-motion: reduce` 下均无溢出或异常滚动。
### 回滚原则

- 不要使用 `git checkout -- .`、`git reset --hard` 或直接覆盖配置文件，因为工作区可能包含协作者的本地编辑。
- 应根据本轮基线只回滚本次新增的代码和 CSS；若无法可靠区分，先保存 `git diff`，再逐块恢复并重新验证。
- 配置文件和图片资源属于独立变更，除非问题明确来自这些文件，否则不要随同页面逻辑一起回滚。

## 验证清单

修改 JSON 后至少确认：

- `npm run check` 通过。
- 浏览器刷新后内容与 JSON 一致。
- `git diff --check` 无空白错误。
- 桌面端和移动端没有明显溢出或错位。

## 相关文档

- `DEVELOPER_GUIDE.md`：完整的项目维护指南。
- `AGENTS.md`：项目维护入口。
